package SeleniumDSLMain;

import SeleniumDSL.*;
//import JsonTools.JsonHandler;
import SeleniumDSLUI.*;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

//import java.io.File;

//import java.util.Random;
//import java.util.Scanner;
//import java.util.concurrent.TimeUnit;

public class SeleniumTester {
	public static void main(String[] args) {
		//JsonReader reader;
		/*final String basePath = new File("").getAbsolutePath();

		JsonHandler jsonHandler;

		if(args.length>0){
			jsonHandler = new JsonHandler(args[0]);				
		} else {
			jsonHandler = new JsonHandler(basePath+"/src/main/resources/RoutineConfigs/targetConf.json");
		}

		System.setProperty("webdriver.chrome.driver", basePath+"/src/main/resources/chromedriver");
		WebDriver driver = new ChromeDriver();

		driver.get("https://www.google.com/");
		driver.manage().timeouts().implicitlyWait(2, TimeUnit.SECONDS);*/

		SeleniumHandler seleniumHandler = new SeleniumHandler();
		//Scanner input = new Scanner(System.in);
		
		DSLParser parser = new DSLParser(seleniumHandler);
		parser.parse();
		parser.run();

		//long seed = System.currentTimeMillis();
                //Random randomGenerator = new Random(seed);

		//System.out.println("Ready");
		//InputWaiter.waitForInput("start");

		//Scanner input = new Scanner(System.in);
		/*while(true){
			//input.next();
			System.out.println("in while");
			executer.executeCommands();
			try{
				Thread.sleep(4000*+randomGenerator.nextInt(30));
			} catch(Exception e){

			}
			//executer.executeCommands();
		}*/
	}
}
